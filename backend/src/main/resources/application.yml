spring:
  datasource:
    url: jdbc:mysql://localhost:3300/campus_travel_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.campus.travel.entity
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: campus_travel_platform_secret_key_2024_designed_by_ai_agent
  expiration: 86400000 # 24 hours

logging:
  level:
    com.campus.travel: debug
server:
  port: 8088

